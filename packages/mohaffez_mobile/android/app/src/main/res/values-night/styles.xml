<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Launch theme (dark mode). AppCompat DayNight handles edge-to-edge correctly on API 35+. -->
    <style name="LaunchTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <!-- Applied once the Flutter engine is running. Flutter manages all insets from here. -->
    <style name="NormalTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
    <!-- Native image cropper screen. uCrop 2.2.11+ handles system-bar insets. -->
    <style name="MohaffezCropperTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    </style>
</resources>
